# diplomahub

A simple static site for diploma students (HTML/CSS files). This repository was created from a local workspace and contains the project's HTML and CSS assets.

## Contents
- HTML pages: `index.html`, `home.html`, etc.
- Stylesheets: `style.css`, `m.css`, `s.css`, `video.css`, `exam.css`, `videos loging.css`
